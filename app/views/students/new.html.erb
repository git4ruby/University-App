<% if @student.errors.any? %>
  <%= pluralize(@student.errors.count, "error") %>
  prohibited your profile from being created
  <ul>
    <% @student.errors.full_messages.each do |message| %>
      <li> <%= message %>
    <% end %>
  </ul>
<% end %>
<%= render 'form' %>